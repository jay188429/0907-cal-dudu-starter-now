# 2026-09-07 예약 시나리오 수정 검수

## 수정 결과

- 현재 후보의 버전만 고객/관리자 목록과 확정 판정에 사용합니다. 이전 후보는 고객 화면의 접힌 이력으로 별도 표시합니다.
- 한국 현재 시각에 시작한 슬롯을 선택·신청·확정할 수 없습니다. 미확정 후보가 모두 시작됐거나 마감되면 조회 시 재선택 필요로 표시합니다.
- 날짜는 OS 시간대에 관계없이 2026-09-09~22, 42슬롯입니다. 고정 시각은 자동 검사에서만 사용합니다.
- 재선택 필요 상태에서만 재제출하며 새 후보 접수 순번으로 정렬합니다. 확정 고객의 추가 신규 신청도 차단합니다. 기존 테스트 하나가 확정 후 추가 예약 성공을 기대하던 것을 PRD에 맞게 거절 기대값으로 수정했습니다. 실패 테스트를 삭제하지 않았습니다.
- sql/01_install.sql은 신규 실습 DB용 검수본입니다. 기존 sql/00_supabase.sql과 42슬롯 fixture를 보존했습니다. 신규 SQL은 서버 시각, 현재 후보 버전, RPC 권한 및 공개 슬롯 정보 범위를 반영합니다.

## 실제 실행한 검사

- `npm ci --offline`: lockfile로 144개 패키지 설치 성공. 의존성 및 lockfile 변경 없음.
- `npm test`: 기존 15개 + PRD 회귀 5개, 총 20개 통과.
- `TZ=UTC npm test`, `TZ=America/Los_Angeles npm test`: 각각 20개 통과.
- `npm run build`: TypeScript 검사 및 Vite 빌드 성공.
- Supabase PostgreSQL 17.6.1.166 이미지의 네트워크 없는 임시 Docker 컨테이너에서 신규 설치 SQL 실행 성공.
- tests/sql/scenario.sql: 6단계, 버전 2의 후보 소진, 재선택 재시도, 고객의 관리자 확정 거절, 테이블 직접 쓰기 권한 차단, 익명 슬롯 조회 42개 확인.
- 같은 SQL 검사에서 실제 authenticated DB 역할로 RPC 호출, 9/9 08:59:59 KST 신청 성공, 09:00:00 확정 거절 및 재선택 성공 확인.
- SQL 재설치 후 슬롯 42개 유지, 설치 전의 검사 신청 보존 확인. 운영 DB에는 연결하지 않았습니다.
- 브라우저에서 현재 한국 시각 표시, C01 오전·오후 선택 및 제출 후 신청 화면을 확인했습니다. C02 선택·제출 후 관리자 화면으로 이동하는 과정에서 브라우저 제어 연결이 끊겨 전체 브라우저 시나리오는 완료하지 못했습니다. 자동 업무 로직/SQL 검사 결과와 구분합니다.

## 재현

로컬 검사는 프로젝트 폴더에서 `npm ci`, `npm test`, `npm run build` 순서로 실행합니다. 시각을 기기에 맞추기 위해 실제 앱 코드나 환경 변수에 고정값을 넣지 않습니다.

SQL 검사는 **별도의 비어 있는 검사 DB**에서 다음 순서로 실행합니다.

1. Auth 서비스가 없는 Supabase PostgreSQL 컨테이너에 한해 tests/sql/bootstrap.sql을 supabase_admin으로 실행합니다. 이 파일은 테스트 JWT 조회 함수만 제공하며 실제 로그인 검증이 아닙니다.
2. sql/01_install.sql을 postgres로 실행합니다.
3. tests/sql/scenario.sql을 `psql -v ON_ERROR_STOP=1`로 실행합니다. 검사는 트랜잭션 내 고정 시각/데이터를 롤백합니다.

## 미검증 및 배포 기준선

실제 Supabase 로그인, 브라우저와 Supabase 연결, 다중 사용자 부하/동시성, 배포와 도메인 연결은 검증 범위에 포함하지 않습니다. 화면은 여전히 로컬 단일 브라우저 수업용입니다. SQL의 Auth 검사는 테스트용 JWT claims와 DB 역할을 사용했으며 실제 인증 서비스와의 통합 시험은 아닙니다. 이 수정은 전체 PRD 보안/저장 규약 인증이 아닙니다.

수정 전 SHA256 검사에서 src/utils/operations.ts와 tests/operations.test.ts가 이미 불일치했습니다. 당시 배포 ZIP은 이 작업에 제공되지 않아 배포 원본과의 내용 비교는 수행하지 못했습니다. SHA256.json을 갱신하지 않았으며 `node scripts/check-manifest.mjs`는 이번 수정 파일을 포함한 차이를 실패 종료 코드로 보고합니다. 강사의 원본 비교·감독 검수 후 별도 기준선 배포가 필요합니다.
